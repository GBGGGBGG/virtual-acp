{
  "name": "gatex-acp",
  "version": "0.1.0",
  "private": true,
  "description": "GateX ACP - Adaptive Deterministic Risk Operating System",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "node --env-file=.env --watch src/server.js",
    "test": "node --test",
    "preflight": "node --env-file=.env scripts/preflight.js",
    "smoke": "node --env-file=.env scripts/smoke.js",
    "simulate:historical": "node --env-file=.env scripts/simulate-historical.js",
    "benchmark": "node --env-file=.env scripts/benchmark.js",
    "benchmark:summary": "node --env-file=.env scripts/benchmark-aggregate.js --mode external --limit 3",
    "case-study:synthetic": "node --env-file=.env scripts/case-study-synthetic.js",
    "bundle": "bash scripts/build-deployment-bundle.sh",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ioredis": "^5.6.1",
    "zod": "^3.23.8"
  }
}