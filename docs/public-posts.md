# GateX Public Post Templates

## 1) X / Twitter (short)
GateX ACP is live: a deterministic risk gate for autonomous agents.

- Explainable allow/deny
- Bounded adaptive tuning (no random decisions)
- Policy rollback + version history
- Signed verification metadata
- Reproducible benchmark artifacts

Docs: [insert link]

## 2) Notion / Blog (mid)
### GateX ACP
GateX is a deterministic execution-risk layer for autonomous agents.
It does not replace strategy; it guards execution with explainable, reproducible controls.

**Highlights**
- Deterministic gate pack
- Adaptive (bounded) tuning
- Policy versioning + rollback
- Simulation/report/export
- Runtime metric (`processing_ms`) and benchmark artifacts

**Objective status**
- Local benchmark + regression workflow in place
- Public methodology documented
- Next: deployment-like infra benchmark + external case study

Docs:
- One-pager: `docs/public-onepager.md`
- Quickstart: `docs/quickstart.md`
- Benchmarks: `docs/benchmarks/`

## 3) Discord / Community
Built a new ACP module: **GateX** — deterministic risk gating for autonomous agents.

What’s included:
- reproducible allow/deny decisions
- bounded adaptive tuning
- rollback + version history
- preflight/smoke/benchmark scripts
- JS/Python integration examples

Current published numbers are local-host benchmarks (good for regression).
Deployment-like benchmark + real case study are next.

If you want, I can share a 5-minute quickstart path.
